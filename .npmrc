# @firebase/rules-unit-testing@5 declares a peer of firebase@^12 while the app
# pins firebase@^9. This is a dev-only test dependency; legacy resolution lets
# both local and Cloudflare Pages `npm install` succeed without --legacy-peer-deps.
legacy-peer-deps=true
